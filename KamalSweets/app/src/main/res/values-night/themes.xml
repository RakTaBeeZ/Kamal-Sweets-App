<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.KamalSweets" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="android:statusBarColor">@color/colorPrimary</item>
        <item name="colorSurface">@color/colorPrimary</item>
        <item name="colorOnSurface">@color/white</item>
    </style>
    <style name="ThemeOverlay.ActionBar" parent="">
        <item name="colorPrimary">@color/colorPrimary</item>
    </style>
    <style name="Buttoncolor">
        <item name="android:backgroundTint">@color/buttonColor</item>
        <item name="android:textColor">@color/white</item>
    </style>
    <style name="cardBackground">
        <item name="android:backgroundTint">@color/Dark_Grey</item>
        <item name="android:textColor">@color/white</item>
    </style>
    <style name="textColor">
        <item name="android:textColor">@color/white</item>
    </style>
    <style name="background">
        <item name="android:background">@color/Dark_Grey</item>
    </style>
</resources>